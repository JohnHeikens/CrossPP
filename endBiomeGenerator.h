#pragma once
#include "biomeGenerator.h"
struct endBiomeGenerator :biomeGenerator
{
	endBiomeGenerator(const biomeID& identifier);
	//the maximum radius of the islands it generates at a certain y level
};